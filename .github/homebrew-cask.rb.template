# Template Homebrew cask. Copy into your tap repo at:
#   pdrbrnd/homebrew-tap/Casks/tomo.rb
#
# Update `version` and `sha256` per release (or automate via the release
# workflow — see https://github.com/macauley/action-homebrew-bump-cask
# for one PR-based approach).

cask "tomo" do
  version "1.0.0"
  sha256 "REPLACE_WITH_SHA256_FROM_RELEASE_ARTIFACT"

  url "https://github.com/pdrbrnd/tomo/releases/download/v#{version}/Tomo-v#{version}.dmg"
  name "Tomo"
  desc "Native macOS e-book library manager"
  homepage "https://github.com/pdrbrnd/tomo"

  livecheck do
    url :url
    strategy :github_latest
  end

  depends_on macos: ">= :tahoe" # macOS 26+

  app "Tomo.app"

  zap trash: [
    "~/Library/Application Support/com.pdrbrnd.tomo",
    "~/Library/Preferences/com.pdrbrnd.tomo.plist",
    "~/Library/Caches/com.pdrbrnd.tomo",
  ]
end
